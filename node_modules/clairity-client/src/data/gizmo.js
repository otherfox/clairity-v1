import Immutable, {Record} from 'immutable'

const GizmoRecord = Record({
  id: null,
  name: ''
});

const GizmoQueries = {
  byName(name) {
    return [];
  },
  all() {
    return [
      new GizmoRecord({name: 'haha'}),
      new GizmoRecord({name: 'g2'})
    ];
  }
};

export { GizmoRecord, GizmoQueries };
