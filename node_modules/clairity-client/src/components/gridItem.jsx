// Layout Grid

import React from 'react'

var GridItem = React.createClass({
  
  componentDidMount: function () {
    
    var width = React.findDOMNode(this).offsetWidth;
    var cols = this.props.cols || 12;
    var widths = this.props.widths.lg || this.props.children.length;
  
    var children = React.Children.map(this.props.children, function(child, i) {
        var width = this.getChildWidth(i);
        console.log(width);
        return React.cloneElement(child);
    }, this);
    
    console.log(width, cols, widths);
  },
  
  componentDidMount: function () {  
    var width = React.findDOMNode(this).offsetWidth;
    var cols = this.props.cols || 12;
    var widths = this.props.widths || this.props.children.length;
  },
  
  style: {
    backgroundColor: '#333333',
    color: "#ffffff",
    float: "left"
  },
  
  render() {
    return (
      <div style={this.style}>
        {this.props.children}
      </div>
    );
  }

});


export default GridItem;