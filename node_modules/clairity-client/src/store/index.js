
//import Store from 'immutable-store'

import schema from './schema'
let Store = function(schema) {

};

let appStore = new Store(schema);
appStore.MessageTypes = Store.MessageTypes;

export default appStore;
