import data from './stubs/aging'
import _ from 'lodash'
import moment from 'moment'

import req from 'superagent'

export function requestAgingReports() {
  new Promise((s, f) => {
    req.get("http://52.4.5.42:80/controller.cfm?event=serialize&authkey=tardis&_c=ample.dao.UserDAO&_m=getUserById&id=1")
      .withCredentials()
      .end((err, res) => {
        console.log(err);
        console.log(res);
      });
  });
  return new Promise((res, rej) => _.defer(() => res(data)));
}

export function requestAgingReportsSince(date) {
  throw new Error('Not yet implemented.');
}
